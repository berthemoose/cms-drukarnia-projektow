# ----------------------------------------------------------------------
# 1. SECURITY & ENVIRONMENT (CRITICAL)
# ----------------------------------------------------------------------
# Ignore all environment variable files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Exception: Always allow the example file for team reference
!include.env.example
!.env.example

# SSL/TLS Keys and Certificates
*.pem
*.pub
*.key
*.cert
*.crt
*.ca-bundle
*.p12
*.pfx

# Encryption keys / secrets
*.keystore
*.jks

# ----------------------------------------------------------------------
# 2. DEPENDENCIES
# ----------------------------------------------------------------------
node_modules/
.pnpm-store/

# ----------------------------------------------------------------------
# 3. PAYLOAD CMS & BUILD ARTIFACTS
# ----------------------------------------------------------------------
# Payload build output (default is usually 'dist' or 'build')
dist/
build/

# Payload 3.0+ / Next.js artifacts (if using Payload with Next.js)
.next/
out/

# Cache directories
.cache/
.payload-cache/

# ----------------------------------------------------------------------
# 4. MEDIA & UPLOADS
# ----------------------------------------------------------------------
# If using the local disk storage adapter, do not commit user uploads.
# Update 'media' to match the slug of your upload collections if different.
media/
uploads/
/public/media/

# ----------------------------------------------------------------------
# 5. MONGODB & LOCAL DATA
# ----------------------------------------------------------------------
# Local MongoDB data (if you spin up DBs inside the project root - rare but risky)
data/
db/

# MongoDB Dump/Restore files (prevents leaking database snapshots)
dump/
mongod.lock
*.bson
*.gzip
*.archive

# ----------------------------------------------------------------------
# 6. LOGS & DEBUGGING
# ----------------------------------------------------------------------
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# ----------------------------------------------------------------------
# 7. IDE & SYSTEM FILES
# ----------------------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.swp
*.swo
Thumbs.db
ehthumbs.db
